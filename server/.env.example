# Database Configuration
# This is where your notes will be stored
MONGODB_URI=mongodb://localhost:27017/notebook

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend Configuration
# This is the URL of your React app
FRONTEND_URL=http://localhost:3000

# Security (you can change this to any random string)
JWT_SECRET=your-super-secret-jwt-key-here

# If you want to use MongoDB Atlas (cloud database) instead of local MongoDB:
# 1. Go to https://cloud.mongodb.com
# 2. Create a free account and cluster
# 3. Get your connection string
# 4. Replace the MONGODB_URI above with something like:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/notebook?retryWrites=true&w=majority
